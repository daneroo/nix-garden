# Legacy Experiments

`nix-garden/` preserves the repository's earlier installer, Disko, Clan,
Proxmox, Incus, and non-NixOS experiments with their Git history.

This material is evidence and reference, not the live architecture. Harvest a
useful idea into the root workflow before reviving it. Do not reuse the public
test-VM password hash.
