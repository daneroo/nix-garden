# Quick utilities

Status: done

Goal: complete the backlog's small shared CLI and current-state improvements on
`main`.

- [x] Add `lazygit`, `doggo`, and the `dnsutils` package providing `dig` to both
      hosts. `[tier: low]`
- [x] Add `scripts/current-state.sh` and `just current-state` to report the
      running NixOS revision beside the repository branch and dirty-aware
      description. `[tier: low]`
- [x] Run `just check`, close both backlog items, and record the outcome.
      `[tier: low]`
