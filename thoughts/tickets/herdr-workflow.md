# Herdr workflow

Install upstream Herdr v0.7.3 as a NixOS system package on `hardy`, then
exercise a detached session and its Codex integration. Keep upstream inputs
unmodified on the first pass; defer persistence, clipboard, and remote attach
configuration until the baseline workflow is verified.
