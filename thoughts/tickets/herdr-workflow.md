# Herdr workflow

Install upstream Herdr v0.7.5 as a NixOS system package on `hardy`, then
exercise a detached remote session and native Codex/Hermes detection. Keep
upstream inputs unmodified on the first pass; defer persistent configuration,
clipboard behavior, and declarative Codex-hook ownership until the baseline
workflow is verified.
